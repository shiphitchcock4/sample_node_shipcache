# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  ci:
    - ps -eax
    - shippable_cache_container
    - ls -al
  cache: true

#integrations:
#  hub:
 #   - integrationName: ship-docker
  #    type: docker
